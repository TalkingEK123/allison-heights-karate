/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://allisonheightskarate.ca',
  generateRobotsTxt: true, // also generates /robots.txt
  // optional nice-to-haves:
  // changefreq: 'weekly',
  // priority: 0.7,
  // exclude: ['/404', '/500'],
};
